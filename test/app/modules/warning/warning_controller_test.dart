import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_aws_project/app/modules/warning/warning_controller.dart';
import 'package:flutter_aws_project/app/modules/warning/warning_module.dart';

void main() {
  initModule(WarningModule());
  // WarningController warning;
  //
  setUp(() {
    //     warning = WarningModule.to.get<WarningController>();
  });

  group('WarningController Test', () {
    //   test("First Test", () {
    //     expect(warning, isInstanceOf<WarningController>());
    //   });

    //   test("Set Value", () {
    //     expect(warning.value, equals(0));
    //     warning.increment();
    //     expect(warning.value, equals(1));
    //   });
  });
}
